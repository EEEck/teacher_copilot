# Azure deployment (placeholder)

Reserved for a future Azure-hosted beta or production stack. No configs are checked in yet.

When added, follow the same layout as [`deploy/railway/`](../railway/) (provider folder → `backend/` + `frontend/` service artifacts + operator README) and reuse the beta env matrix documented in [`deploy/README.md`](../README.md).
